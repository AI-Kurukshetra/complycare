import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // "standalone" is used for Docker builds only.
  // Remove it for Vercel — Vercel manages its own output format.
};

export default nextConfig;
